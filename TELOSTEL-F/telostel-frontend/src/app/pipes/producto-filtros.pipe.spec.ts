import { ProductoFiltrosPipe } from './producto-filtros.pipe';

describe('ProductoFiltrosPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoFiltrosPipe();
    expect(pipe).toBeTruthy();
  });
});
